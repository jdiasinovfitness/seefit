import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter', // TODO: check this
  appName: 'seeFit',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
